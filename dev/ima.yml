version: '2'

services:
    pgsql_94:
        networks:
            - ima

networks:
    ima:
        driver: bridge
        ipam:
            driver: default
            config:
                - subnet: 172.22.0.0/24
